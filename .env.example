# Production site URL (required).
# Used by astro.config.mjs (site, sitemap, robots) and src/config/site.ts
# (canonical, OG, JSON-LD) — single source of truth, no silent fallback.
# Copy to .env and set your production domain:
SITE_URL=https://example.com
